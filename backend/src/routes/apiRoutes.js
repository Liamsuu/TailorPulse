import { Router, Request, Response } from "express";
const apiRouter = Router();
apiRouter.post("/analyse", (req, res) => {
    res.send("test");
});
export default apiRouter;
//# sourceMappingURL=apiRoutes.js.map